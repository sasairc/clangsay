language: c
compiler:
    - gcc
    - clang
before_install:
    - sudo apt-get update
    - sudo apt-get -y install pkg-config glib2.0 libglib2.0-dev
    - gcc --version
    - clang --version
script:
    - make `pkg-config --cflags --libs glib-2.0`
    - make clean
