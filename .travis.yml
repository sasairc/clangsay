language: c
compiler:
    - gcc
    - clang
before_install:
    - sudo add-apt-repository ppa:lyrasis/precise-backports -y
    - sudo apt-get update
    - sudo apt-get install pkg-config glib2.0 libglib2.0-dev
    - pkg-config pkg-config --libs glib-2.0 
script:
    - make
    - male clean
