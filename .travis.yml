language: c
install:
    - sudo apt-get update
    - sudo apt-get -y install glib2.0 libglib2.0-dev
compiler:
    - gcc
    - clang
script:
    - clang --version
    - make CC="clang" && make clean
    - gcc --version
    - make && make clean
